import React from 'react';

interface Scores {
    [key: string]: number;
}

const scores: Scores = {
    '✊': 1,
    '✋': 1,
    '✌': 1,
};

console.log(scores)


const RPS = () => {
    
    return (
        <div>
            sadsad
        </div>
    );
};

export default RPS;