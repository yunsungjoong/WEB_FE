import React from 'react';

interface Scores {
    [key: string]: number;
}

const scores: Scores = {
    '✊': 1,
    '✋': 0,
    '✌': -1,
};

console.log(scores)


const RPS:React.FC = () => {
    
    return (
        <div>
            
        </div>
    );
};

export default RPS;